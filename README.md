# Лабораторная работа 2. VecSum

**Выполнил:** Буданов Максим, гр. 6131  
**Среда выполнения**: Kaggle   
Использовалась библиотека `Numba` для доступа к возможностям CUDA на языке Python.

---

## Задача

Реализовать алгоритм сложения элементов вектора (редукция):
- Входные данные: Вектор размером от 1 000 до 1 000 000 значений.
- Реализация: 
    - Последовательный вариант на CPU.
    - Параллельный вариант на GPU с применением CUDA.
- Выходные данные: Сумма элементов, время вычисления, расчет ускорения.

---

## Реализация

В работе реализовано два подхода к вычислению суммы:

- **CPU** (`cpu_sum`) — классический последовательный проход по массиву с накоплением суммы в одной переменной. Сложность составляет $O(N)$.
- **GPU** (`reduce_sum_kernel` + `final_reduce_kernel`) — параллельная реализация с использованием **древовидной редукции** (tree-based reduction).
    - **Что распараллелено:** Процесс суммирования разбит на блоки. Каждый поток в блоке суммирует элементы и сохраняет результат в разделяемую память (`shared_memory`). Затем внутри блока происходит каскадное сложение (stride reduction), пока не останется одно значение на блок.
    - **Почему это эффективно:** Вместо последовательного сложения $N$ элементов, мы выполняем операцию за $\log N$ шагов. Использование `shared_memory` позволяет минимизировать обращения к глобальной памяти GPU, что значительно ускоряет процесс.
    - **Особенности:** используется двухуровневая редукция: сначала основные блоки вычисляют частичные суммы, затем финальный кернел собирает итоговый результат.

---

## Результаты

- Результаты вычислений

<img width="455" height="271" alt="pandas_table" src="https://github.com/user-attachments/assets/faaff2da-58b7-44e6-b4bc-bf98455aa6f5" />

- График времени вычисления и график ускорения **GPU** относительно **CPU**

<img width="1490" height="629" alt="graphics" src="https://github.com/user-attachments/assets/b12208ea-50f0-43bd-92d5-9ef896c616ae" />
---

## Выводы

1.  **CPU** — показывает линейный рост времени выполнения при увеличении размера вектора. На малых данных (N=1000) он может быть конкурентоспособен из-за отсутствия накладных расходов на пересылку данных.
2.  **GPU** — демонстрирует колоссальное преимущество на больших массивах. Согласно полученным данным, на векторе в 1 000 000 элементов удалось достичь ускорения порядка **267x** по сравнению с последовательным кодом.
3.  **Стабильность** — разница в результатах вычислений (Difference) между CPU и GPU крайне мала ($\approx 10^{-8} - 10^{-13}$), что подтверждает корректность работы параллельного алгоритма и использования типа `float64`.
4.  **Накладные расходы** — при малых N (например, 1000) ускорение меньше единицы ($0.22x$), так как время на инициализацию CUDA-контекста и копирование данных в память видеокарты превышает время самого расчета.
